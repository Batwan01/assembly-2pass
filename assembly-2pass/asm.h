#include "optab.h"

extern int asm_space(Dctab* dp);
extern int len_space(Dctab* dp); //len_drctv.c
extern int asm_mnemonic(Optab* op); //asm_mnemonic
extern int len_mnemonic(Optab* op); //len_mnmo.c

